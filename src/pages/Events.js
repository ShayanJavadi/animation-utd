import React from "react";
import Link from "gatsby-link";

const EventsPage = () => (
  <div>
    <h1>
      events
    </h1>
    <Link to="/">
      Go back to the homepage
    </Link>
  </div>
);

export default EventsPage;
